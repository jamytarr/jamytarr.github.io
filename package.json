{
  "name": "shopify-to-hugo-exporter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Export Shopify store data into a Hugo static site",
  "scripts": {
    "export": "node src/export-shopify.mjs",
    "build": "hugo --minify"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "cheerio": "^1.2.0",
    "pixelmatch": "^7.1.0",
    "playwright": "^1.58.2",
    "pngjs": "^7.0.0"
  }
}
